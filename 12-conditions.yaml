- name: demo on conditions
  hosts: local
  connection: local
  vars:
    IS_LIVE: true
  tasks:
  - name: check the stream live or not
    ansible.builtin.debug:
      msg: "Yes, it is Live"
    when: IS_LIVE